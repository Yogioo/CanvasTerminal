- [x] 新增可配置起始命令的终端节点
  - [x] 比如默认创建成终端节点后自动输入 pi 命令
  - [x] 比如默认创建成终端节点后自动输入 'canvas done ((git status -sb | Out-String))' 命令然后直接返回控制台信息给下一个节点

- [x] 将右侧的面板全部删除, 相关逻辑和代码删除, 因为他是用于调试的, 不是给用户看的, 完全没有价值, 删除相关代码
- [x] 删除 Ctrl + S 对应功能

- [x] 序列化与反序列化
  - [x] 保存图成配置文件
  - [x] 读取配置文件并生成图

- [ ] 终端的全屏功能

- [ ] 多终端节点Agent合作
  - [ ] deep-interview 澄清需求，确定边界，非目标，技术约束
  - [ ] ralplan 审批计划， 方案权衡，实施路径。 需求变成可执行的task列表
    - [ ] team 当任务需要并行， 用这个， 并行开发任务， 每个链路调用 ralph 串行流程
    - [ ] ralph 当任务需要串行的时候，用这个，进行持久处理任务 
      - [ ] 独立worktree， 可持久化，可resume

- [ ] 需要实现并行分流节点, 支持单个节点基于连线, 分发给所有的下游节点
- [ ] 需要实现判断分流节点, 支持单个节点基于连线, 发送给选择的单个下游节点
